package com.bro.learning.coreAPI.Object_Big;

// Objects 是一个静态类,不允许被实例化

public class ObjectsDemo {
    // Object类中的 equals() 在对象为空时就会报错
    // 当然,我们可以手动添加判断语句,不过 Objects 给我们提供了一些方法
    static void main() {
        /*

        if (Objects.equals(obj1, obj2)) {
            // 安全比较，即使obj1或obj2为null
        }

        */

        // 当然,还有其他方法
    }
}
