abs(double a) double 返回参数的绝对值

abs(float a) float 返回参数的绝对值

abs(int a) int 返回参数的绝对值

abs(long a) long 返回参数的绝对值

max(double a, double b) double 返回两个参数中的较大值

max(float a, float b) float 返回两个参数中的较大值

max(int a, int b) int 返回两个参数中的较大值

max(long a, long b) long 返回两个参数中的较大值

min(double a, double b) double 返回两个参数中的较小值

min(float a, float b) float 返回两个参数中的较小值

min(int a, int b) int 返回两个参数中的较小值

min(long a, long b) long 返回两个参数中的较小值

pow(double a, double b) double 返回a的b次幂

sqrt(double a) double 返回参数的平方根

cbrt(double a) double 返回参数的立方根

exp(double a) double 返回自然常数e的a次幂

log(double a) double 返回参数的自然对数（以e为底）

log10(double a) double 返回参数的常用对数（以10为底）

ceil(double a) double 向上取整，返回大于等于参数的最小整数

floor(double a) double 向下取整，返回小于等于参数的最大整数

round(double a) long 四舍五入，返回最接近参数的long值

rint(double a) double 返回最接近参数的double值，中间值向偶数舍入

random() double 返回[0.0, 1.0)之间的随机数

sin(double a) double 返回角度的正弦值（参数为弧度）

cos(double a) double 返回角度的余弦值（参数为弧度）

tan(double a) double 返回角度的正切值（参数为弧度）

asin(double a) double 返回参数的反正弦值，结果在-π/2到π/2之间

acos(double a) double 返回参数的反余弦值，结果在0.0到π之间

atan(double a) double 返回参数的反正切值，结果在-π/2到π/2之间

toRadians(double angdeg) double 将角度转换为弧度

toDegrees(double angrad) double 将弧度转换为角度

PI double 数学常量π，近似值为3.141592653589793

E double 自然常数e，近似值为2.718281828459045
